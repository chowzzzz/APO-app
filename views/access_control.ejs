<% include partials/header %>
<% include partials/nav %>

<%
function UNIXConverter(UNIX_timestamp){
  try {
    if (UNIX_timestamp == 0) {
      return "";
    }
    var a = new Date(UNIX_timestamp * 1000);
    var months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    var year = a.getFullYear();
    var month = a.getMonth() + 1;
    var date = a.getDate();
    var hour = a.getHours();
    var min = a.getMinutes();
    var sec = a.getSeconds();
    var time = date + '/' + month + '/' + year;
    return time;
  } catch (error) {
    return "";
  }
}
%>
    <div class="container-fluid">
      <div class="row">
        &nbsp;
      </div>
      <button class="btn btn-primary" onclick="window.location.href='/general_screener/<%=officer_details.nric%>'">View General Screener</button>
      <hr>
      <h4>Access Control: <%=officer_details.nric%></h4>
      <hr>
      <table class="table table-hover">
        <thead>
          <th>Status</th>
          <th>Certified Date</th>
          <th>Theory</th>
          <th>HHMD</th>
          <th>PDS</th>
        </thead>
        <tbody>
          <% if (ac_details == null) { %>
            <tr>
              <td colspan="5" style="text-align:center;">Officer does not have any existing certificate.</td>
            </tr>
          <% } %>
          <% for (var access_control_id in ac_details) { %>
            <% var status; %>
            <% if (ac_details[access_control_id].overall_status == "1") { status = "PASS"; } else { status = "FAIL"; } %>
            <tr class='clickable-row' data-href='/access_control/<%=officer_details.nric%>/<%=access_control_id%>'>
              <td><%=status%></td>
              <td><%=UNIXConverter(ac_details[access_control_id].certified_date)%></td>
              <td><%=UNIXConverter(ac_details[access_control_id].theory.date)%></td>
              <td><%=UNIXConverter(ac_details[access_control_id].hhmd.date)%></td>
              <td><%=UNIXConverter(ac_details[access_control_id].pds.date)%></td>
            </tr>
          <% } %>
        </tbody>
      </table>
      <hr>
      <% if (user.role == 1) { %>
      <button class="form-control btn btn-primary" onclick="window.location.href='/access_control/new/<%=officer_details.nric%>'">Add New Record</button>
      <% } %>

    </div>

<% include partials/footer %>
